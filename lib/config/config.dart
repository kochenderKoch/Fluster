/// All static [Config] settings for the Application
class Config {
  /// Random Constant
  static const String random = '';

  /// is the logging detailed?
  static const bool isDetailedLogging = true;

  /// How many items are stored in the logger history?
  static const int loggerHistory = 100;

  /// Splashscreen Animation Duration
  static const Duration animationDuration = Duration(seconds: 5);
}
